#include <iostream>
#include <string>
#include <sstream>
using std::endl;
using std::cout;
using string;

#define ARRAYSIZE 10;
string table[ARRAYSIZE] = {"zero", "one", "two", "three", "four", "five", "six", "seven", "eight", "nine"};
class Solution {
public:
    string countAndSay(int data) {
	 sstream ss;
	 ss << data;
	 string val;
	 ss >> val;

    }
};
